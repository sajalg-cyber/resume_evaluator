[project]
name = "resume-shortlister"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sajalg-cyber", email = "sjg4374@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "groq>=1.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
resume-shortlister = "resume_shortlister:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
